# PenMark 多轮启发式 Bug 搜寻提示词

> 这个任务是**递归式的**：每轮发现 Bug 后 → 分析根因模式 → 举一反三 → 下一轮用新模式扩大搜寻范围。你要自己跑完 N 轮，直到找不出新 Bug 或模式用尽为止。

---

## 第一轮：表面扫描（模式发现）

### 1.1 跨模块引用一致性扫描

用以下手法逐一检查 `server.js`、`auth.js`、`ai.js`、`database/`、`desktop/` 中所有 `require` 和 `module.exports`：

- 扫描每个文件中 `require('./xxx')` 和 `require('../xxx')` 的调用
- 在目标模块中搜索对应的 `exports.xxx` 或 `module.exports = { xxx }`
- 重点检查：**函数名拼写**（单复数、驼峰 vs 下划线、缩写歧义）
- 重点检查：**导入后未使用**的 require（写了但没用到）

```
示例模式：服务器调用 auth.readShareCookies(req) 但 auth.js 只导出 readShareCookie
→ 类别：「导出/导入名称不匹配」
```

### 1.2 异步调用安全性扫描

- 搜索所有 `async function` → 检查每个函数体内 await 表达式数量 → 如果函数声明了 async 但内部没有 await，标记（可能是遗留重构残留）
- 搜索所有 `.then(` / `.catch(` → 检查是否每个 Promise 链都有错误处理
- 搜索所有 `new Promise(` → 检查是否能 resolve 和 reject 都被调用
- 搜索所有 `AbortController` / `AbortSignal` → 检查哪些异步操作**应该有但没有** cancel 机制
- 检查所有 `setTimeout` / `setInterval` → 是否有对应的 `clearTimeout` / `clearInterval`（防止内存泄漏）

```
示例模式：sendAiMessage() 调用 api() 没传 signal，但 runAiRewrite() 有 aiAbortController
→ 类别：「同类异步操作 cancel 支持不一致」
```

### 1.3 前端 DOM 引用安全性扫描

- 搜索所有 `getElementById(` → 检查是否有对返回值做 null 检查
- 搜索所有 `querySelector(` → 同样检查 null 安全
- 搜索所有 `.innerHTML =` → 检查是否有 XSS 风险（用户输入是否经过 escapeHtml）
- 搜索所有 `document.createElement(` → 检查创建后是否都 append/insert 到了 DOM 中

```
示例模式：renderAiPresetRow 中 $('aiPresetRow') 返回 null 时后续代码崩溃
→ 类别：「DOM 引用未做空值防护」
```

### 1.4 CSS 类名一致性扫描

- 从 `index.html`、`share.html`、`login.html` 提取所有 `class="xxx"` 和 JS 中 `.className = 'xxx'`
- 从 `styles.css` 提取所有 `.classname {` 定义
- 找出 HTML/JS 中用了但 CSS 中不存在的类名（死类）
- 找出 CSS 中定义了但 HTML/JS 中没用的类名（死样式）

### 1.5 硬编码与配置泄露

- 搜索所有带 `http://` / `https://` 的字符串 → 检查是否应该走环境变量
- 搜索所有 API 路径字符串 `/api/` → 检查是否有遗漏别名的端点
- 搜索 `console.log(` / `console.warn(` / `console.error(` → 分类：哪些是调试残留、哪些是正常的错误日志

### 1.6 错误处理完整性扫描

- 搜索所有 `catch` 块 → 检查是否有**空的 catch 块**（`catch(e) {}`）
- 搜索所有 `try {` → 检查 try 块内是否有多条可能抛错的语句，第二条之后被吞掉
- 检查所有 Express 路由 → 是否每个都有 `wrap()` 包装？有没有直接写成 async 的？
- 检查所有 `res.status(xxx).json(` → 搜索是否有**缺少 return** 的情况（发了响应但继续执行后续代码）

---

## 第二轮：根因分析 + 举一反三

> 对第一轮发现的每个 Bug 做归类，提取出**根因模式**，然后用该模式全仓搜索同类问题。

### 2.1 命名不一致模式（如果第一轮发现了）

如果第一轮发现了命名不一致（如 readShareCookies vs readShareCookie），做以下扩大搜索：

- 搜索所有 `auth.xxx(` 调用 → 对照 auth.js 确认每个方法名都存在
- 搜索所有 `db.xxx(` 调用 → 对照 database/index.js 确认
- 搜索所有模块的 `module.exports` → 对照所有 require 它们的文件，逐方法验证
- 检查 app.js 中所有 `api('/api/xxx')` 路径 → 对照 server.js 确认路由都存在

### 2.2 缺少 Cancel 支持模式（如果第一轮发现了）

如果第一轮发现有的异步操作没 cancel，把所有带 `fetch(` / `api(` / `XMLHttpRequest` 的地方列出来，逐一判断是否需要 cancel：

- 用户主动触发的网络请求 → 应该有 cancel（如 AI 对话）
- 自动保存 → 不需要 cancel（太快了）
- 后台预加载 → 可 cancel（节省带宽）

### 2.3 异常吞没模式（如果第一轮发现了）

如果第一轮发现空 catch 块或 try 里多条语句：

- 全仓搜索 `catch\s*\(\s*\w*\s*\)\s*\{\s*\}`（空 catch）
- 全仓搜索 `try {` 并手动检查 try 块行数是否 >3，且第 2-3 行可能抛错
- 检查所有中间件（`app.use`）的错误处理是否完整

### 2.4 数据流完整性

- 检查所有 `db.query(` / `db.one(` / `db.execute(` 的 SQL 返回 → 是否有对空结果的 null/undefined 检查
- 检查所有数组遍历（`forEach` / `map` / `filter`）→ 数组是否可能为 null/undefined
- 检查所有字符串截断（`.slice(0, N)` / `.substring`）→ 是否可能切到多字节字符中间

---

## 第三轮：深度模式挖掘

> 前两轮消化完后，开始挖掘更深层的模式。

### 3.1 竞态条件扫描

- 搜索所有全局状态变量（`let xxx =` 在模块顶层）→ 列出所有并发可能修改的情况
- 检查 `saveCurrent()` 和 `saveCurrentInBackground()` 的调用时机 → 是否有并发保存互踩
- 检查所有 `setTimeout` 回调 → 回调执行时状态是否可能已经被后面的操作改变
- 搜索 `switching` / `saving` / `loading` 等状态标志 → 检查是否有**忘记重置**的路径（try 里设置了 true，但 catch 提前 return 没重置）

### 3.2 内存泄漏模式

- 搜索所有 `addEventListener(` → 检查是否有对应的 `removeEventListener`
- 搜索所有 `new MutationObserver(` → 检查是否有 `disconnect()`
- 搜索所有 `new IntersectionObserver(` → 检查是否有 `disconnect()`
- 搜索所有在 `setInterval` 内创建的闭包 → 检查闭包是否持有大对象引用
- 检查编辑器切换文档时 → 旧文档的 DOM 事件监听是否被清理

### 3.3 桌面端/网页端路径差异

- 搜索所有 `process.env.PENMARK_DESKTOP` → 列出每个分支的行为差异
- 检查是否有功能在桌面端正常但网页端异常（或反过来）→ 重点看路径处理、认证、静态文件

### 3.4 边界值敏感点

- 搜索所有 `.slice(0, N)` / `.substring(0, N)` → N 值是否太小，是否截断了有效内容
- 搜索所有 `MAX_` / `_LIMIT` 常量 → 检查当前值是否合理
- 搜索所有排序 `ORDER BY` → 检查是否有未建索引的列被用于排序
- 检查所有输入长度限制 → 是否只在前端做了校验但后端没做（或反过来）

---

## 第四轮：用户体验状态机

> 检查每个 UI 组件是否覆盖了所有状态。

### 4.1 状态覆盖率检查

对于每个 UI 组件（按钮、面板、对话框、列表），检查是否处理了以下状态：

| 状态 | 检查项 |
|------|--------|
| **初始** | 首次展示时是否有合理的默认状态（非空白） |
| **加载中** | 是否有 loading 指示器 |
| **空** | 数据为空时是否有友好提示 |
| **错误** | 接口失败时是否有错误提示和重试入口 |
| **成功** | 操作成功后是否有确认反馈 |
| **禁止** | 权限不足时是否有明确提示 |
| **极限** | 数据量极大时是否做分页/虚拟列表 |

### 4.2 交互完整性

- 每个可点击元素是否有 `cursor: pointer`
- 每个按钮是否在 disabled 时有 `cursor: not-allowed` + 样式区分
- 每个表单输入是否有 `placeholder`
- 每个可聚焦元素是否可以通过 Tab 键到达
- 所有 dialog/modal → 是否支持 Esc 关闭 + 点击遮罩关闭

### 4.3 响应式断点覆盖

- 列出所有 `@media` 查询 → 检查是否有遗漏的断点
- 在 360px / 375px / 414px / 768px / 1024px / 1280px / 1440px 各宽度脑检布局
- 检查所有 `position: fixed` 元素 → 是否考虑了 safe-area-inset
- 检查所有 `overflow: hidden` → 是否有内容被意外裁剪

---

## 第五轮：安全审计

### 5.1 输入校验

- 检查所有 `req.body.xxx` 的读取 → 是否有类型转换和空值校验
- 检查所有 URL 参数 `req.query.xxx` → 是否有注入风险
- 检查所有路径参数 `req.params.xxx` → 是否有目录遍历风险（`../`）
- 检查所有文件操作（`fs.readFile`、`fs.writeFile`）→ 路径是否来自用户输入

### 5.2 XSS 防护

- 搜索所有 `innerHTML` → 赋值内容是否来自用户输入、是否经过 escapeHtml
- 搜索所有 `document.write(` → 立即标记
- 搜索所有 `eval(` → 立即标记
- 搜索所有 `new Function(` → 立即标记

### 5.3 认证/授权

- 检查所有 API 端点 → 是否有需要登录但没加认证中间件的
- 检查所有 `/api/public/` 端点 → 确认公开是故意的
- 检查桌面端 vs 网页端的认证差异 → 是否有绕过路径

---

## 第六轮：性能反模式

### 6.1 网络请求

- 搜索所有 `fetch(` / `api(` 调用 → 列出调用频率和触发时机
- 检查是否有**重复请求**（同一数据被多次 fetch）
- 检查是否有请求可以合并（如列表 + 详情分开请求）
- 检查是否有请求可以预加载（如 hover 时提前 fetch）

### 6.2 DOM 操作

- 搜索所有 `innerHTML =` → 检查是否在循环内使用（应该用一次批量赋值）
- 搜索所有 `appendChild` / `insertBefore` → 检查是否逐个追加（应用 DocumentFragment）
- 检查编辑器内容更新时的重新渲染策略 → 是否做了脏检查 / diff

### 6.3 数据库

- 搜索所有 SQL 查询 → 检查是否有 N+1 问题（循环内查询）
- 检查所有 JOIN 查询 → 条件列是否有索引
- 检查事务使用 → JS 层循环内开事务也是 N+1

---

## 第七轮：代码坏味道

### 7.1 重复代码

- 搜索相似度高的函数 → 两个函数如果逻辑 80% 相同，标记为可抽象
- 搜索重复的 CSS 块 → 同样的属性组合出现 3 次以上，应抽象为类
- 搜索相同的字符串常量出现多次 → 应提取为常量

### 7.2 魔法数字

- 搜索所有裸数字出现在代码中（非变量/常量）→ 如 1000、3000、24、48 等
- 判断每个数字的语义 → 是否需要提取为命名常量

### 7.3 注释质量

- 搜索 `// TODO` / `// FIXME` / `// HACK` / `// XXX` → 列出并评估紧急程度
- 搜索被注释掉的代码块 → 判断是应该删除还是恢复

---

## 每轮结束后的动作

完成每轮后，输出一份**分类汇总**：

```
## 第 N 轮发现

### 新增 Bug（共 X 个）
| # | 文件 | 行号 | 严重度 | 描述 |
|---|------|------|--------|------|
| 1 | xxx | xxx | P0 | xxx |

### 新发现的根因模式（共 Y 个）
| 模式名称 | 描述 | 本轮命中数 | 下轮搜索策略 |
|---------|------|-----------|-------------|
| 命名不一致 | xxx | 3 | 扩大搜索所有跨模块调用 |

### Bug 库存变化
- 上轮累计：X 个
- 本轮新增：Y 个
- 本轮修复：0 个（此任务只做发现不做修复）
- 本轮未修复：Z 个
```

---

## 全局约束

1. **只发现不修复**。这是个审计任务，不要改任何文件。
2. **每轮必须产出新的搜索策略**。不能只是复读前一轮的结果。
3. **真实代码为准**。不要根据文件名或函数名猜测，必须读实际代码。
4. **优先 P0/P1**。不要浪费时间在命名风格、代码格式等非功能性问题上。
5. **主动追踪**。发现一个模式后，立即全仓搜索同类问题，不要等到下一轮。
6. **如果某一轮没有任何新发现**，停止迭代，输出最终汇总。
